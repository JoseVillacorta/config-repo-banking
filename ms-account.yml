spring:
  application:
    name: ms-account
  r2dbc:
    url: r2dbc:postgresql://localhost:5434/db_account_dev
    username: postgres
    password: admin
  
server:
  port: 8082
logging:
  level:
    org.springframework.r2dbc: DEBUG
management:
  endpoints:
    web:
      exposure:
        include: health,info,refresh

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true